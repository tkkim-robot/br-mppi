[project]
name = "br-mppi"
version = "0.1.0"
description = "Barrier-rate guided MPPI examples ported from the mobile_arm experiments."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy==1.26.4",
    "matplotlib>=3.8,<3.9",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.8",
]

[tool.uv]
package = false
